image: alpine:latest

services:
  - docker:dind
  
stages:
  - security-tests

  
sourceguard:
  stage: security-tests
  image: sourceguard/sourceguard-cli:v0.0.10
  script:    
    - /sourceguard-cli --src ./ -x "test.sh"