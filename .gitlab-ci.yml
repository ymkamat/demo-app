image: alpine:latest

stages:
  - tests
  
sourceguard:
  stage: tests
  image: sourceguard/sourceguard-cli:latest
  script:
    - ls -al .git/
    - /sourceguard-cli --src ./ -x "test.sh" -x "src/**"   